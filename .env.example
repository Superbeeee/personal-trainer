# ── 复制成 .env 并填好（python-dotenv 会自动载入，不用手动 source）──

# ===== 必填 =====

# 1) Telegram bot token（跟 @BotFather 申请，见 README 步骤一）
TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJklmNOpqrs

# 2) 只允许你自己用：你的 Telegram 数字 ID（见 README 步骤二）
#    填 0 = 不限制（不建议，等于谁加都能用、还能驱动你本机的 claude）
ALLOWED_TELEGRAM_USER_ID=0

# 3) claude 执行档真实路径（claude 多半是 shell alias，subprocess 认不得）
#    用 `which claude` 看 alias 指到哪；mac 装 native build 通常是下面这个
CLAUDE_BIN=/Users/superbeeee/.claude/local/claude

# 4) 权限模式：acceptEdits=自动读写档案、不跑任意 Bash（建议）
#    要让教练也能跑指令就改成 bypassPermissions（风险自负）
PERMISSION_MODE=acceptEdits

# 5) 单次回覆逾时秒数（教练若要跑工具会花点时间）
CLAUDE_TIMEOUT=300

# 6) 免询问就放行的工具。读写档案已由 acceptEdits 放行，这里额外开放：
#    Bash(git:*) = 让教练能依要求 push 训练纪录到 GitHub（只放行 git，安全）
#    要再接 MCP（如 Notion）可空白分隔：例如 "Bash(git:*) mcp__notion"
ALLOWED_TOOLS=Bash(git:*)

# 7) session 闲置自动重开（分钟）：超过这时间没讲话，下一则就开新 session
#    省 token。设 0 = 永不自动重开（维持同一条对话直到 /new）
#    想最省 token、每则都全新（教练不记得上一则）→ 设很小如 0.1
SESSION_IDLE_MINUTES=30

# 8) 教练资料夹：里面有 CLAUDE.md + logs/，训练紀錄写在这
#    预设 coach/ 子资料夹（刻意不含 .env / 程式碼），通常不用改
COACH_DIR=coach

# 9) 模型：留空=用 Claude Code 预设；要指定就填 opus 或 claude-opus-4-8
CLAUDE_MODEL=
